[build]
  base    = "apps/web"
  command = "pnpm install && pnpm build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"
  PNPM_VERSION = "9"

# Next.js runtime — handles SSR, API routes, and image optimization
[[plugins]]
  package = "@netlify/plugin-nextjs"
